# Kinovea 多屏及相关功能修改记录

本文档汇总自对话历史（含 [三屏起步](969b8f7a-a4e6-4737-9000-910f12d16a45) 与后续 `4-display` 迭代）中的**功能向代码改动**。  
说明面向熟悉 C / Java / Python / Bash、但不熟悉 C# / .NET 的读者；文中会用类比帮助理解。

---

## 1. 三屏独立显示（第一阶段）

### 需求

原软件最多只能显示两个工作区屏幕。希望先支持最多 **3 个屏幕**：可独立显示、加载视频/摄像头、拖放、关闭；双屏同步先保持原样（恰好两屏时才启用）。

### 改动点

- 界面容器：从固定左右两栏（`SplitContainer`）改为可动态增减列的表格布局（`TableLayoutPanel`）。
- 屏幕管理：增加上限 `MaxScreens = 3`；用列表下标代替“左屏/右屏”二元判断。
- 关闭菜单：增加第 3 个关闭入口。
- 加载器：视频/摄像头在未满 3 屏时可自动再开一屏。
- 采集缓存：多采集屏时，延时缓存内存按采集屏数量均分（不再写死除以 2）。
- 公共控制：仍仅在恰好 2 屏时显示。

### 对于改动的解释

可以把它想成：原来界面像只开了两个固定“窗口格子”，程序写死了“左边一个、右边一个”。  
现在改成类似 HTML 表格或 Python 里按长度生成的列表：有几个屏就画几列，每个格子知道自己的编号（0、1、2）。  
加载文件时，程序会在列表里找空位；找不到且还没满 3 个，就再新建一格。  
双屏同步像是一个只服务“恰好两个人”的遥控器——第三个人加入时先不启用，避免一次改太多。

---

## 2. 完整三屏 / 四屏架构与可配置布局

### 需求

在单屏、双屏之外，正式增加三屏、四屏模式：支持任意播放/采集组合；三屏横排、四屏默认 2×2；多播放/多采集各自同步；快照与视频按界面布局合成；叠加预览以活动播放屏为底图。

### 改动点

- 引入布局规格 `ScreenLayoutSpec`（屏数、每格类型、行列）。
- 统一入口 `ApplyLayout(...)`，替代零散的“切到 1/2 屏”逻辑。
- 增加配置对话框 `FormScreenLayout`（选屏数与每格类型）。
- 菜单/工具栏增加三播放、四播放、配置布局等入口。
- UI：1～3 屏按列排布，4 屏 2×2；关闭菜单扩到最多 4 项。
- 双播放/双采集控制器泛化为多屏列表；时间轴、发丝线等支持最多 4 路。
- 导出/快照按当前行列合成；混合布局中非播放位可用黑底占位。
- 语言资源（英/中）补充相关文案。

### 对于改动的解释

以前切换模式像 Bash 里写死的几个 `case`：只能跳到“1 屏”或“2 屏”。  
现在有一份“布局说明书”（`ScreenLayoutSpec`）：写清几行几列、每格是播放还是采集。  
`ApplyLayout` 像按说明书重建桌面：该关的关、该建的建、类型不对的替换。  
配置对话框相当于图形化编辑这份说明书。  
同步/导出模块从“只认 A、B 两个人”改成“认一个名单”，名单里有几个就处理几个。

---

## 3. 四屏 1×4 横排布局

### 需求

在已有多屏版本上，再增加一种 **1 行 4 列** 的四屏排布（与 2×2 并存）。

### 改动点

- `ScreenLayoutSpec` 支持显式指定列数/行数（例如 4×1）。
- 菜单与工具栏增加“四屏 1×4”入口。
- 配置布局对话框在屏数为 4 时可选择 2×2 或 1×4。
- 导出/合成路径跟随当前行列设置。

### 对于改动的解释

四屏不一定总是田字格。有时更需要四个画面并排对比。  
就像同样 4 张图，既可以摆成 2×2，也可以摆成一排 4 张；程序用“列数×行数”区分这两种摆法。

---

## 4. 图像叠加：忽略空屏、叠到活动屏、拖拽可动

### 需求

1. 三/四屏时若有空屏，仍应能开叠加；空屏忽略。  
2. 其他屏叠到当前活动屏上。  
3. 拖拽源屏画面时，目标屏上的叠加层跟着移动。

### 改动点

- 同步/叠加改为基于“已加载视频的屏”（`playablePlayers`），不因空屏整体失败。
- 叠加目标取活动/参考播放屏。
- 源屏拖拽过程中持续上报叠加图像（`ReportForSyncMerge`）。

### 对于改动的解释

以前像会议室点名：只要有一个空座位，整场会议就不开始。  
现在只统计“真正坐着人的座位”，空座位跳过。  
叠加目标是“主持人席”（活动屏）：别人的画面贴到他上面。  
拖拽时源屏不断把“当前看到的画面”发给目标，所以目标上的半透明层会跟着动——类似直播推流时源画面一动，预览立刻更新。

---

## 5. 叠加上报性能优化（仅视口变化时上报）

### 需求

拖拽很频繁时，不要每一像素移动都上报，只在视口真正变化时上报，降低 CPU 占用。

### 改动点

- 增加“视口是否变化”判断（如 `ReportForSyncMergeIfViewportChanged`）。
- 未变化则不上报/不重建叠加图。

### 对于改动的解释

像监控文件系统：不必每次鼠标抖一下都写日志，只有窗口范围真变了才通知。  
少做几次“截屏 + 合成”，拖拽更顺滑。

---

## 6. 叠加目标锁定（避免点源屏抢走叠加目标）

### 需求

开启叠加后，在源屏上点击/拖拽时，不要把叠加目标改成源屏本身；目标应保持启用叠加时的那一屏。

### 改动点

- 启用叠加时记录 `mergeTargetPlayer`。
- 合并期间优先使用该锁定目标，直到关闭叠加。

### 对于改动的解释

以前点谁，谁就变成“贴图底板”，等于贴图跟着鼠标跳走。  
现在开启叠加时先钉住底板（目标屏）；源屏只负责提供素材和拖拽位置，不能抢走底板身份。

---

## 7. 多屏切少屏时缓存视频与标注

### 需求

从多屏切到少屏时，被关掉的屏上的视频（以及标注）不要丢；再切回多屏时能自动恢复。

### 改动点

- 缩屏前把槽位描述与标注缓存到临时目录（如 `Temp/layout-slots/*.kva`）。
- 扩屏时按槽位恢复视频并加载标注。
- Home / 手动关闭等路径会清理或平移缓存下标，避免错位。

### 对于改动的解释

像把多余标签页“最小化到后台”而不是直接关死：  
关掉格子前，把“播的是哪个文件、画了什么标注”存到临时文件；格子再开回来就按编号取回。  
编号像数组下标：中间删一格，后面缓存要往前挪，否则会对错号。

---

## 8. 修复运行时缺少 AForge DLL

### 需求

启动后因找不到 `AForge.Video.DirectShow` 等程序集而崩溃。

### 改动点

- 将 AForge 相关 DLL 复制到运行目录。
- 在 `Kinovea.targets` 中增加构建时复制步骤，减少以后再漏拷。

### 对于改动的解释

.NET 程序启动时要加载依赖库（类似 Linux 的 `.so` 或 Windows 的 `.dll`）。  
代码能编译通过，但运行目录少了 DLL，就会 `FileNotFoundException`。  
修法是：确保输出目录里有这些文件，并让构建脚本以后自动带上。

---

## 9. Shift + 拖拽交换两屏内容

### 需求

三屏及以上、且**未**叠加时：按住修饰键（最终定为 **Shift**）把屏 A 内容拖到屏 B，则 A、B 内容互换。

### 改动点

- 播放屏 UI：检测 Shift + 左/中键拖拽，发起拖放。
- 屏幕管理 UI：接收拖放并请求交换。
- `ScreenManager.SwapScreens(i, j)`：交换列表中两个屏幕对象引用。
- 叠加模式下禁用该交互，避免与叠加拖拽冲突。

### 对于改动的解释

列表里两个元素交换位置：`swap(list[i], list[j])`。  
界面格子按列表顺序排布，交换对象后，画面自然对调。  
要求按住 Shift，是为了和普通点击、平移、叠加拖拽区分开（类似“必须按住特殊键才触发的快捷操作”）。

---

## 10. 在活动屏右侧插入屏幕（后改为无上限）

### 需求

增加按钮：在**活动屏右侧**插入一屏，保留原内容；插入后各列等宽。  
随后要求：满 4 屏仍可插入，**无上限**。

### 改动点

- 菜单/工具栏增加“在活动屏右侧插入屏幕”。
- `InsertScreenToRightOfActive()`：在活动下标 +1 处插入空屏（类型与活动屏相同）。
- 插入后强制 **1×N** 等宽列布局。
- 布局槽位缓存从固定数组改为可增长列表。
- `CanAddScreen()` 对插入路径不再卡死在 4；预设布局对话框仍可按最多 4 屏设计。
- 插入后活动屏保持不变；新屏默认空。

### 对于改动的解释

像在电子表格中间插入一列：右边的格子整体右移，中间出现空白列。  
等宽是因为用百分比列宽：`每列 = 100% / N`。  
“无上限”就是列表长度不再被写死成 4；缓存也改成可变长列表，避免插到第 5 屏时数组越界。

---

## 11. 播放屏每屏亮度 / 对比度 / 色温控制块

### 需求

为每个**播放屏**增加独立的亮度、对比度、色温调节（显示层效果，不改相机硬件）。

### 改动点

- 播放屏底部控制区增加三个滑块 + 数值 + 重置。
- `PlayerScreen` 保存每屏参数状态。
- 绘制视频帧时用颜色矩阵（`ColorMatrix` / `ImageAttributes`）做显示级变换。
- 英/中文案与资源访问器同步。

### 对于改动的解释

参数挂在“屏幕对象”上，不是全局一份——多屏互不影响，类似每个播放器进程自己的音量。  
调节时并不改视频文件，只在画到屏幕前做颜色变换（类似看图软件的临时滤镜）。  
色温用红/蓝通道轻微偏移模拟偏暖/偏冷。

---

## 12. 叠加模式下：源屏 Alt+滚轮置顶并调透明；色调同步到叠加层

### 需求

1. ~~叠加模式下，Ctrl + 左键点击源屏 → 目标屏叠加变不透明。~~（已取消）  
2. 改为：在源屏上 **Alt + 滚轮**，将该源屏画面在目标叠加层中移到最上层，并调节该层透明度。  
3. 调节源屏亮度/对比度/色温时，目标屏上的叠加画面同步变化。  
4. **双击源屏**：目标屏仅显示该源的不透明画面；再双击同一源屏则恢复原先多路叠加与透明度。

### 改动点

- 移除 Ctrl+左键“变不透明”交互。  
- 源屏 Alt+滚轮：将该源提到叠放顺序末尾（顶层），并按滚轮增减该源层透明度后重建叠加；同时把目标屏整体 SyncAlpha 置为 1，避免“源层已不透明但仍被总透明度打成半透”。  
- 开启叠加时目标屏 SyncAlpha 也设为 1；各源层默认透明度约 0.5。  
- 目标屏 Alt+滚轮：仍可再拧整体叠加透明度（原逻辑）。  
- 源屏空白处双击：进入/退出 solo（仅该源、不透明）；切换其他源双击则换到该源 solo；Alt+滚轮会先退出 solo 再调透明度。  
- 叠加合成为带 Alpha 的分层绘制（后画的在上层）；每帧上报不再打乱已有叠放顺序。  
- 色调滑块变化时调用 `ReportForSyncMerge()`；克隆叠加图时走同一套色调绘制。

### 对于改动的解释

多路叠加像 Photoshop 图层：Alt+滚轮点在某一“源图层”上时，先把它提到最上面，再拧它的不透明度旋钮。  
双击则像 solo 某一轨：目标屏只看这一路不透明画面，再双击退出 solo 回到之前的叠放状态。  
若源层调到不透明仍盖不住目标屏画面，通常是目标还在用默认约 50% 的总透明度画整张贴纸——已改为以源层透明度为准。  
在目标屏上滚轮，则拧的是整张贴纸的总透明度。  
色调仍是源屏先调好，再把调好的画面贴过去。

---

## 13. 图像复位、工作区会话文件、每屏文件搜索

### 需求

1. 亮度/对比度/色温可一键复位到默认（0/0/0）。  
2. 把当前屏幕布局 + 每屏视频 + 亮度/对比度/色温保存成文件，打开后可恢复。  
3. 每屏顶部搜索框：历史下拉、完整路径打开、关键字在最近文件夹与常用目录中搜索并在当前屏打开。

### 改动点

- 修复复位按钮与色温数值标签重叠，按钮移到色温值右侧。  
- 扩展 workspace XML（FormatVersion 1.1）：`Columns` / `Rows`；每屏 `Brightness` / `Contrast` / `ColorTemperature`。  
- 「选项 → 工作区 → 打开…」加载会话；导出/存为默认会带上新字段；启动/打开时按网格恢复布局，加载后应用 B/C/T。  
- 每屏顶部搜索条：`ComboBox` + 打开；`FileSearchHelper` 在最近文件目录、`我的视频`、`文档` 中按文件名关键字搜（限深度/结果数/时间）。

### 对于改动的解释

工作区文件像一份「桌面快照」：几块屏怎么排、每块打开了什么、色调拧到哪。  
搜索框像带历史的地址栏：能贴路径，也能用关键字在常去文件夹里找片，找到后直接灌进当前这一屏。

---

## 14. 搜索增强、Home 挂起、最多屏 / 刚关屏恢复

### 需求

1. 每屏搜索下拉可滚动、可拖动调整大小，便于看完整路径。  
2. 关键字优先在最近打开文件夹与可配置常用目录（Shortcuts）中搜，再在固定盘全盘搜。  
3. Home：挂起当前多屏（不销毁）；再按 Home 回到屏幕。若在浏览器中打开文件，则销毁挂起会话并开新屏。  
4. 按钮：恢复本会话「屏幕数量最多」时的布局与内容。  
5. 按钮：恢复刚刚关闭的那一屏。

### 改动点

- `FileSearchDropDown`：可调整大小的 ListBox 弹出层（横向/纵向滚动）。  
- `RecentFolders` 写入偏好；`FileSearchHelper` 两阶段搜索。  
- Home 改为 `screensSuspended` 挂起/恢复；浏览器开文件走 `DiscardSuspendedScreensForNewFile`。  
- 会话 peak 快照与关闭栈（含描述 + 临时 .kva）；工具栏/菜单「恢复最多屏」「恢复刚关闭的屏」。

### 对于改动的解释

Home 像把整桌「最小化」到浏览器，屏还在后台；只有你从浏览器里新开文件，才清空旧桌面。  
「最多屏」记住你这轮会话曾经摆开的最大阵仗；「刚关屏」像浏览器的重新打开标签页。

---

## 15. 2×2 左右分侧控制面板

### 需求

在 **2×2** 布局下：左上 / 左下的进度条、播放钮、亮度等控制条靠屏幕左侧；右上 / 右下靠右侧。  
**1 行 2 列**及其它布局仍保持底部控制条，不做侧边改动。

### 改动点

- `ScreenManager.OrganizeScreens` 在 `Identify` 后对 `PlayerScreen` 调用 `ApplyControlsLayout(index, columns, rows)`。  
- 仅当 `columns == 2 && rows == 2`：左列 `Dock.Left`、右列 `Dock.Right`；否则恢复 `Dock.Bottom`。  
- `PlayerScreenUserInterface2`：侧边模式竖向重排工作区/进度/播放/速度/导出与 B/C/T；底部布局可还原；侧栏宽度约 200px。

### 对于改动的解释

四宫格时，左右两列的控制条贴各自外侧，中间视频区少被底栏「对切」挡住；两屏横排仍用原来的底栏，避免窄屏再被侧栏挤扁。

---

## 16. 每屏控制条显示 / 隐藏切换

### 需求

在每屏搜索框左侧增加按钮，可切换该屏播放控制条（进度条、亮度等）的显示与隐藏；默认显示。

### 改动点

- `PlayerScreenUserInterface2`：搜索条左侧 `btnToggleVideoControls`（Dock Left），切换 `panelVideoControls.Visible`。  
- 按钮状态：`▾` 表示已显示、`▸` 表示已隐藏；中英文 tooltip。  
- 切换 2×2 左右布局时不强制重新显示，保留用户隐藏状态。

### 对于改动的解释

像把底栏/侧栏「收起来」腾出画面；需要调进度或亮度时再点开。每屏独立，互不影响。

---

## 17. 多屏打开视频 / 退出时的「假重载」优化

### 需求

1. 在某一屏打开视频时，其它已打开屏不应明显重新解码或跳回起点。  
2. 退出软件时不应逐屏 `CloseFile` 并反复重排剩余屏。

### 改动点

- `DualPlayerController.ResetSync(preservePositions)`：加载后的 `SelectionChanged` / `ResetAsked` 走软同步，重建公共时间线但不对全体 `GotoTime(0)`。  
- `LoaderVideo.LoadVideo`：只停目标屏，去掉全局 `RaiseStopPlayback`。  
- `PrepareScreenCells`：屏集合与网格未变时跳过拆除重建；`ScreenListChanged` 在同一批 Player 实例时跳过 Exit/Enter。  
- `CloseSubModules`：脏标注一次确认后批量 `RemoveScreen`，中间不调用 `CloseFile` / `OrganizeScreens` / 会话关屏栈。

### 对于改动的解释

以前像每开一张新片就把整桌其它片子都「倒带重贴」一遍，关软件也像一张张撕掉再把剩下的重排。  
现在开片只动那一格的解码，同步只重新对齐时间尺；关软件则整桌一次性收起。

---

## 18. 最近文件 / 文件夹 / 搜索输入历史上限调至 1000

### 需求

最近文件夹、最近文件、搜索框输入历史的保留上限均调整为 1000。

### 改动点

- `RecentFolders`：`maxRecentFolders = 1000`。  
- `RecentFiles`：默认 `MaxRecentFiles = 1000`；读取旧偏好时若为 1–999 则升为 1000（0 仍表示关闭历史）。  
- 搜索输入历史：`MaxSearchHistory = 1000`。  
- 偏好里「最近历史条数」下拉改为 0/10/20/50/100/200/500/1000。

### 对于改动的解释

搜索优先扫过的「常去文件夹」和可点的历史列表都能记住更多条，少丢冷门路径。

---

## 19. 「恢复最多屏」改为记住峰值期间最新内容

### 需求

恢复本会话屏幕数量最多时的布局与内容时，应恢复**达到最多屏数之后、减屏之前**的最后一次完整内容（含后来在新屏上打开的视频），而不是刚插入空屏那一刻。

### 改动点

- `MaybeUpdatePeakSnapshot`：屏数 **≥** 当前峰值时刷新快照（原先仅在屏数 **严格大于** 峰值时拍摄）。  
- `LoaderVideo` 加载成功后调用 `RefreshPeakSnapshotIfAtPeak`，使开片也会更新同峰值快照。

### 对于改动的解释

以前像只拍了「桌子刚加到最宽」那一张空桌照；现在同宽度下每开一片就更新全家福，减屏后再恢复能回到关屏前的样子。

---

## 20. workspace 导入恢复多屏

### 需求

导出为多屏的 workspace XML，再次导入时也应按文件中的屏描述完整恢复，而不是被固定上限截断。

### 改动点

- `ScreenLayoutSpec` 去掉运行时的固定 4/5 屏校验。  
- `ScreenManager.ApplyLaunchScreenDescriptions` 不再按 `MaxScreens` 裁掉多余描述，因此 workspace / CLI / 自动启动描述会完整导入。  
- `FormScreenLayout` 改为按需动态增行，屏数输入可继续增大，不再固定只显示前 4/5 行。
- 批量导入 workspace / AutoLaunch 期间，`LoaderVideo` / `LoaderCamera` 不再中途逐屏 `OrganizeScreens`；待所有屏描述装载完成后，再按 workspace 中的 `Columns/Rows` 一次性排版，避免 `1×4` 被中途默认成 `2×2`。

### 对于改动的解释

以前 workspace 明明记下了更多屏，但导入流程一开始就按固定上限把后面的描述砍掉了。  
现在多屏描述会完整进入运行时，布局窗也会随着屏数增加而补出更多行；像 `1×4` 这样的布局也会在全部屏恢复后按原网格一次性落位。

---

## 附录：主要涉及模块（便于对照代码）

| 模块 | 大致职责（类比） |
|------|------------------|
| `ScreenManager.cs` | 总控：布局、挂起、peak/关屏恢复、加载工作区 |
| `ScreenManagerUserInterface.cs` | 主界面格子与拖放、浏览器/屏切换显示 |
| `ScreenLayoutSpec.cs` / `FormScreenLayout.cs` | 布局说明书与配置窗 |
| `DualPlayerController.cs` | 多播放同步、叠加合成调度 |
| `PlayerScreen` / `PlayerScreenUserInterface2` | 单个播放屏及其控件、绘制、搜索条 |
| `Workspace` / `ScreenDescriptionPlayback` | 会话 XML 读写 |
| `FileSearchHelper` / `FileSearchDropDown` / `FormFileSearchResults` | 搜片与可调下拉 |
| `SessionScreenSnapshot` | peak / 关屏快照 |
| `LoaderVideo` / `LoaderCamera` | 打开文件/摄像头时选哪一格 |
| `ImageHelper` | 多图合成（导出/叠加） |
| `Kinovea.targets` | 构建时复制依赖 DLL |

---

## 说明

- 对话中大量 Git / 环境 / 编译工具问答未列入本文（不属于产品功能改动）。  
- 部分能力在演进中发生过迭代（例如屏数上限从 3→4→插入无上限；叠加目标从“跟随活动”到“启用时锁定”），以上按**最终意图与落地行为**描述。  
- 分支脉络大致为：`3-display`（三屏起步）→ `4-display`（四屏与后续增强）。
